export const enum BackgroundAnimationType {
	NONE,
	PARTICLES
}

export const enum ParticleAnimationMode {
	NONE,
	COLOR,
	GRAVITY
}

export const enum GravityParticleType {
	NONE,
	ATTRACTIVE,
	REPULSIVE
}

export const DIST_LINK = 90; // Distance below which two particles can interact
